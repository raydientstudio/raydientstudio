import { Main } from "@/components";
import PageNotFound from "./(errors)/not-found";

const NotFound = () => {
    return (
        <Main>
            <PageNotFound />
        </Main>
    )
}

export default NotFound